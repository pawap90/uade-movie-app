export const showSpinner = {
	type: 'SHOW_SPINNER'
};

export const hideSpinner = {
	type: 'HIDE_SPINNER'
};

export const login = {
	type: 'LOGGED_IN'
};

export const logout = {
	type: 'LOGGED_OUT'
};